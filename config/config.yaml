
server:
  port: 8080
  mode: debug

database:
  host: localhost
  port: 3306
  user: root
  password: root
  dbname: library_db
  charset: utf8mb4
  max_open_conns: 100
  max_idle_conns: 10

jwt:
  secret: init-secret-key
  expire: 720

log:
  level: info
  filename: logs/app.log
